import React from 'react';
import Title from "../title/Title";

const Schools = ({title}) => {
    return (
        <div>
            <h1>{title}</h1>
        </div>
    );
};

export default Schools;